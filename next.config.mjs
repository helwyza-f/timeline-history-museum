/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "crmsgchfvmltrwgnvszr.supabase.co",
      },
    ],
  },
};

export default nextConfig;
